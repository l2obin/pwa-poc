import type { SiteConfig } from "@/types";

export const siteConfig: SiteConfig = {
    name: 'PWA PoC',
    description: 'A proof of concept for a Progressive Web App using React, TanStack Router, and SQLite.'
}
